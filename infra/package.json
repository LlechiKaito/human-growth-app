{
  "name": "infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "synth": "cdk synth",
    "diff": "cdk diff",
    "build:web": "npm -w @human-growth/web run build:export",
    "deploy": "npm run build:web && cdk deploy --all --require-approval broadening",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint lib bin"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.165.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^20.17.6",
    "aws-cdk": "^2.165.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
